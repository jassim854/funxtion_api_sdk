part of funxtion_sdk;

class GetFitnessActivityTypeRequest {
  Future<FitnessActivityTypeModel?> getFtinessActivityTypeById(
      String id) async {
        return null;
      
//         NetwoerkHelper netwoerkHelper=NetwoerkHelper();
//  netwoerkHelper.   _dioCacheManager = DioCacheManager(CacheConfig());
//     dio.interceptors.add(netwoerkHelper. _dioCacheManager?.interceptor);
//     var response = await netwoerkHelper. dio.get(
//       ConstantApis.getFitnessActivityTypeApi + id,
//       options: buildCacheOptions(const Duration(days: 7), forceRefresh: true),
//     );
//     if (response.statusCode == 200) {
//       FitnessActivityTypeModel m =
//           FitnessActivityTypeModel.fromJson(response.data);

//       return m;
//     }
//     return null;
  }
}
